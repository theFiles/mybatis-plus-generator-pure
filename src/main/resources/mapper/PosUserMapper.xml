<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lidaye.mybatis.plus.generator.mapper.PosUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lidaye.mybatis.plus.generator.entity.PosUser">
        <id column="id" property="id" />
        <result column="nick_name" property="nickName" />
        <result column="phone" property="phone" />
        <result column="password" property="password" />
        <result column="sex" property="sex" />
        <result column="user_photo" property="userPhoto" />
        <result column="user_disable" property="userDisable" />
        <result column="create_date" property="createDate" />
        <result column="remarks" property="remarks" />
        <result column="number" property="number" />
        <result column="agent_id" property="agentId" />
        <result column="salt" property="salt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, nick_name, phone, password, sex, user_photo, user_disable, create_date, remarks, number, agent_id, salt
    </sql>

</mapper>
